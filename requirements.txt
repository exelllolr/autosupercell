# Core Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
h11>=0.14.0
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator>=2.2.0  # For EmailStr validation (2.1.0 yanked on PyPI)
beautifulsoup4==4.13.3
httpx==0.26.0

# Browser Automation — только Patchright (undetected Playwright). playwright-stealth не ставим (конфликт с patchright).
patchright>=1.0.0

# Task Queue
arq==0.25.0
redis==5.0.1

# HTTP Client
httpx==0.26.0
aiohttp==3.9.1
requests==2.31.0  # For simple examples and testing

# AI/ML for product search
openai==1.12.0
anthropic==0.34.2  # For Claude
# google-generativeai: используем >=0.8.3 вместо точной версии — поддерживает gemini-1.5-flash/pro.
# ПРИМЕЧАНИЕ: пакет google-generativeai устаревает в пользу google-genai,
# но смена требует рефакторинга gemini_provider.py (другой import и API).
# Оставляем google-generativeai до явного перехода на google-genai.
google-generativeai>=0.8.3
pillow==10.2.0

# Image Processing
# opencv-python-headless используется вместо opencv-python:
#   - не требует libGL / libgthread (GUI-библиотеки X11)
#   - подходит для серверов и Docker без дисплея
#   - полностью совместим по API с opencv-python
opencv-python-headless==4.9.0.80
numpy==1.26.3

# Monitoring
prometheus-client==0.19.0

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
loguru==0.7.2
tenacity==8.2.3

# Database (optional, for order tracking)
sqlalchemy==2.0.25
alembic==1.13.1

# Security
cryptography==42.0.0

# Proxy management
aiohttp-socks==0.8.4

# reCAPTCHA solving (optional, for Supercell login)
2captcha-python==2.0.2
